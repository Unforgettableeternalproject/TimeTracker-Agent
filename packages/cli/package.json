{
  "name": "@timesheet-agent/cli",
  "version": "0.1.0",
  "description": "CLI tool for exporting and managing timesheet data",
  "bin": {
    "timesheet": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@timesheet-agent/core": "workspace:*",
    "commander": "^11.1.0",
    "exceljs": "^4.4.0",
    "date-fns": "^3.0.6",
    "chalk": "^4.1.2",
    "inquirer": "^8.2.6"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7"
  }
}
